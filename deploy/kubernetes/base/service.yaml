apiVersion: v1
kind: Service
metadata:
  name: hearthboard
  labels:
    app.kubernetes.io/name: hearthboard
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hearthboard
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
